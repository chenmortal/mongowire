Hex dumps copied from https://github.com/FerretDB/wire testdata (Apache-2.0).
Format: standard hexdump (offset, hex bytes, ASCII column).
handshake{1..6}_{header,body}.hex: OP_QUERY isMaster handshakes;
import.hex / msg_fuzz1.hex: OP_MSG samples (fuzz one intentionally malformed).
